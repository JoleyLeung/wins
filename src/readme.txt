# start web server type that into the terminal of the vs code
npm run dev 

#build web server (press the plus sign to run a new terminal)
npm run build

#deploy web server
.\node_modules\.bin\gh-pages -d dist

#put it in GitHub by pressing the blue buttons and put in the summary

#check https://joleyleung.github.io/wins/#/ to see if it worked! :D
